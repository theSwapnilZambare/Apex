public class Parklocator{
   public static List < String > country(String country) (
       Parkservice. ParksImp LPort prk5vc = new ParkService. ParksImplPort();
       return prkSvc.byCountry(country);
   }